# =============================================================================
# trading-ci 环境变量配置示例文件
# =============================================================================
# 使用说明：
# 1. 复制此文件为 .env: cp .env.example .env
# 2. 根据实际环境修改配置值
# 3. 实盘交易前请仔细检查所有参数，特别是风控相关配置
# =============================================================================

# =============================================================================
# 基础配置
# =============================================================================

# 运行环境：dev（开发）/ prod（生产）
# 实盘建议值：prod
ENV=dev

# 时区设置
# 实盘建议值：Asia/Dubai（迪拜时区，UTC+4）
TIMEZONE=Asia/Dubai

# =============================================================================
# 数据库配置（必填）
# =============================================================================

# PostgreSQL 数据库连接 URL
# 格式：postgresql://用户名:密码@主机:端口/数据库名
# 注意：数据库名包含 "-" 时需要用双引号创建：CREATE DATABASE "trading-ci";
# 实盘建议值：使用独立的交易数据库，确保高可用性
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/trading-ci

# Redis 连接 URL（用于 Redis Streams 消息队列）
# 格式：redis://[:密码@]主机:端口/数据库编号
# 实盘建议值：使用独立的 Redis 实例，确保高可用性和持久化
REDIS_URL=redis://localhost:6379/0

# =============================================================================
# Redis Streams 配置
# =============================================================================

# Redis Streams 消费者组名称
# 实盘建议值：bot-group（默认值即可）
REDIS_STREAM_GROUP=bot-group

# Redis Streams 消费者名称（同一组内可多个消费者）
# 实盘建议值：bot-consumer-1（可根据实例数量递增）
REDIS_STREAM_CONSUMER=bot-consumer-1

# =============================================================================
# Bybit API 配置（实盘交易必填）
# =============================================================================

# Bybit API Key（从 Bybit 账户设置中获取）
# 实盘建议值：使用只读+交易权限的 API Key，不要使用提现权限
# 安全提示：不要将 API Key 提交到代码仓库
BYBIT_API_KEY=

# Bybit API Secret（从 Bybit 账户设置中获取）
# 实盘建议值：与 API Key 配对使用
# 安全提示：不要将 API Secret 提交到代码仓库
BYBIT_API_SECRET=

# Bybit REST API 基础 URL
# 实盘建议值：https://api.bybit.com（主网）
# 测试网：https://api-testnet.bybit.com
BYBIT_BASE_URL=https://api.bybit.com

# Bybit REST API 基础 URL（执行服务专用）
# 实盘建议值：https://api.bybit.com（主网）
BYBIT_REST_BASE_URL=https://api.bybit.com

# Bybit 公共 WebSocket URL（市场数据）
# 实盘建议值：wss://stream.bybit.com/v5/public/linear（主网）
BYBIT_WS_PUBLIC_URL=wss://stream.bybit.com/v5/public/linear

# Bybit 私有 WebSocket URL（订单/持仓/钱包）
# 实盘建议值：wss://stream.bybit.com/v5/private（主网）
BYBIT_WS_PRIVATE_URL=wss://stream.bybit.com/v5/private

# Bybit 私有 WebSocket 是否启用
# 实盘建议值：true（启用后可减少 REST API 轮询压力，提高实时性）
BYBIT_PRIVATE_WS_ENABLED=false

# Bybit 私有 WebSocket 认证路径
# 实盘建议值：/realtime（默认值）
BYBIT_PRIVATE_WS_AUTH_PATH=/realtime

# Bybit 私有 WebSocket 订阅主题（逗号分隔）
# 可选值：order,execution,position,wallet
# 实盘建议值：order,execution,position,wallet（订阅所有主题）
BYBIT_PRIVATE_WS_SUBSCRIPTIONS=order,execution,position,wallet

# Bybit API 接收窗口（毫秒）
# 实盘建议值：5000（默认值，建议不要设置太小）
BYBIT_RECV_WINDOW=5000

# Bybit 账户类型
# 可选值：UNIFIED（统一账户）/ CONTRACT（合约账户）
# 实盘建议值：UNIFIED（统一账户）
BYBIT_ACCOUNT_TYPE=UNIFIED

# Bybit 交易类别
# 可选值：linear（永续合约）/ inverse（反向合约）
# 实盘建议值：linear（USDT 永续合约）
BYBIT_CATEGORY=linear

# Bybit 持仓模式索引
# 0：单向持仓，1：双向持仓
# 实盘建议值：0（单向持仓，更安全）
BYBIT_POSITION_IDX=0

# Bybit 钱包币种
# 实盘建议值：USDT（默认值）
BYBIT_WALLET_COIN=USDT

# =============================================================================
# Bybit API 限流配置（重要：避免触发限流）
# =============================================================================

# 公共接口限流：每秒请求数（RPS）
# 实盘建议值：8.0（保守值，避免触发限流）
BYBIT_PUBLIC_RPS=8.0

# 公共接口限流：突发请求数
# 实盘建议值：16.0（RPS 的 2 倍）
BYBIT_PUBLIC_BURST=16.0

# 私有接口限流（关键操作）：每秒请求数
# 关键操作包括：下单、撤单、设置止损等
# 实盘建议值：3.0（保守值）
BYBIT_PRIVATE_CRITICAL_RPS=3.0

# 私有接口限流（关键操作）：突发请求数
# 实盘建议值：6.0（RPS 的 2 倍）
BYBIT_PRIVATE_CRITICAL_BURST=6.0

# 私有接口限流（查询操作）：每秒请求数
# 实盘建议值：2.0（保守值）
BYBIT_PRIVATE_QUERY_RPS=2.0

# 私有接口限流（查询操作）：突发请求数
# 实盘建议值：4.0（RPS 的 2 倍）
BYBIT_PRIVATE_QUERY_BURST=4.0

# 私有接口限流（订单查询）：每秒请求数
# 实盘建议值：2.0（默认值）
BYBIT_PRIVATE_ORDER_QUERY_RPS=2.0

# 私有接口限流（订单查询）：突发请求数
# 实盘建议值：4.0（默认值）
BYBIT_PRIVATE_ORDER_QUERY_BURST=4.0

# 私有接口限流（账户查询）：每秒请求数
# 实盘建议值：2.0（默认值）
BYBIT_PRIVATE_ACCOUNT_QUERY_RPS=2.0

# 私有接口限流（账户查询）：突发请求数
# 实盘建议值：4.0（默认值）
BYBIT_PRIVATE_ACCOUNT_QUERY_BURST=4.0

# 按交易对限流（查询）：每秒请求数
# 实盘建议值：0.7（默认值，避免多交易对时压力过大）
BYBIT_PRIVATE_PER_SYMBOL_QUERY_RPS=0.7

# 按交易对限流（查询）：突发请求数
# 实盘建议值：1.5（默认值）
BYBIT_PRIVATE_PER_SYMBOL_QUERY_BURST=1.5

# 按交易对限流（订单查询）：每秒请求数
# 实盘建议值：0.6（默认值）
BYBIT_PRIVATE_PER_SYMBOL_ORDER_QUERY_RPS=0.6

# 按交易对限流（订单查询）：突发请求数
# 实盘建议值：1.2（默认值）
BYBIT_PRIVATE_PER_SYMBOL_ORDER_QUERY_BURST=1.2

# 按交易对限流（账户查询）：每秒请求数
# 实盘建议值：0.6（默认值）
BYBIT_PRIVATE_PER_SYMBOL_ACCOUNT_QUERY_RPS=0.6

# 按交易对限流（账户查询）：突发请求数
# 实盘建议值：1.2（默认值）
BYBIT_PRIVATE_PER_SYMBOL_ACCOUNT_QUERY_BURST=1.2

# 按交易对限流（关键操作）：每秒请求数
# 实盘建议值：1.0（默认值）
BYBIT_PRIVATE_PER_SYMBOL_CRITICAL_RPS=1.0

# 按交易对限流（关键操作）：突发请求数
# 实盘建议值：2.0（默认值）
BYBIT_PRIVATE_PER_SYMBOL_CRITICAL_BURST=2.0

# 限流器最大等待时间（毫秒）
# 超过此时间将降级处理
# 实盘建议值：5000（默认值）
BYBIT_RATE_LIMIT_MAX_WAIT_MS=5000

# 限流器低状态阈值
# 实盘建议值：2（默认值）
BYBIT_RATE_LIMIT_LOW_STATUS_THRESHOLD=2

# 是否仅查询活跃交易对的私有接口
# 实盘建议值：true（减少不必要的查询压力）
BYBIT_PRIVATE_ACTIVE_SYMBOLS_ONLY=true

# =============================================================================
# Bybit API 缓存配置（减少 API 调用）
# =============================================================================

# 钱包余额缓存 TTL（秒）
# 实盘建议值：1.0（默认值，平衡实时性和 API 压力）
BYBIT_WALLET_BALANCE_CACHE_TTL_SEC=1.0

# 持仓信息缓存 TTL（秒）
# 实盘建议值：1.0（默认值）
BYBIT_POSITION_CACHE_TTL_SEC=1.0

# 订单实时状态缓存 TTL（秒）
# 实盘建议值：0.5（默认值，订单状态需要更实时）
BYBIT_ORDER_REALTIME_CACHE_TTL_SEC=0.5

# 未完成订单列表缓存 TTL（秒）
# 实盘建议值：0.5（默认值）
BYBIT_OPEN_ORDERS_CACHE_TTL_SEC=0.5

# =============================================================================
# 市场数据服务配置
# =============================================================================

# 订阅的交易对列表（逗号分隔）
# 实盘建议值：根据策略选择，例如：BTCUSDT,ETHUSDT
# 注意：不要订阅过多交易对，会增加系统负载
MARKETDATA_SYMBOLS=BTCUSDT

# 订阅的时间框架（逗号分隔）
# 可选值：15m,30m,1h,4h,8h,1d
# 实盘建议值：15m,30m,1h,4h,8h,1d（完整时间框架）
# 注意：8h 由 1h 聚合生成，不是 Bybit 原生时间框架
MARKETDATA_TIMEFRAMES=15m,30m,1h,4h,8h,1d

# 是否启用 REST API 回填（启动时）
# 实盘建议值：true（确保数据完整性）
MARKETDATA_ENABLE_REST_BACKFILL=true

# REST 回填每个 (symbol,timeframe) 的最大 K 线数量
# 实盘建议值：500（默认值，上限 1000）
MARKETDATA_BACKFILL_LIMIT=500

# 是否启用缺口回填
# 实盘建议值：true（确保数据连续性）
MARKETDATA_GAPFILL_ENABLED=true

# 缺口回填最大 K 线数量
# 实盘建议值：2000（默认值）
MARKETDATA_GAPFILL_MAX_BARS=2000

# =============================================================================
# 策略配置
# =============================================================================

# 风险百分比（每笔交易的风险比例）
# 例如：0.005 表示账户余额的 0.5%
# 实盘建议值：0.005（0.5%，保守）到 0.01（1%，适中）
# 警告：不要设置过大，避免单笔交易风险过高
RISK_PCT=0.005

# 最大同时持仓数（默认值）
# 实盘建议值：3（默认值，根据策略和资金量调整）
# 注意：同时持仓过多会增加风险暴露
MAX_OPEN_POSITIONS_DEFAULT=3

# 最大同时持仓数
# 实盘建议值：3（默认值，与 MAX_OPEN_POSITIONS_DEFAULT 保持一致）
MAX_OPEN_POSITIONS=3

# 最小确认数（信号确认所需的 K 线数量）
# 实盘建议值：2（默认值，平衡信号质量和延迟）
MIN_CONFIRMATIONS=2

# 自动交易时间框架（逗号分隔）
# 策略会自动在这些时间框架上生成交易计划
# 实盘建议值：1h,4h,1d（默认值）
AUTO_TIMEFRAMES=1h,4h,1d

# 监控时间框架（逗号分隔）
# 用于监控和信号生成，但不自动交易
# 实盘建议值：15m,30m,8h（默认值）
MONITOR_TIMEFRAMES=15m,30m,8h

# 交易计划 TTL（K 线数量）
# 超过此数量的 K 线后，未执行的交易计划将过期
# 实盘建议值：1（默认值）
TRADE_PLAN_TTL_BARS=1

# 信号 TTL（K 线数量）
# 超过此数量的 K 线后，信号将过期
# 实盘建议值：1（默认值）
SIGNAL_TTL_BARS=1

# =============================================================================
# 市场状态配置
# =============================================================================

# 是否启用市场状态标记
# 实盘建议值：false（默认值，除非需要市场状态功能）
MARKET_STATE_ENABLED=false

# 市场高波动阈值（百分比）
# 实盘建议值：0.04（4%，默认值）
MARKET_HIGH_VOL_PCT=0.04

# 市场状态：是否在正常状态时也发送事件
# 实盘建议值：false（默认值，减少事件数量）
MARKET_STATE_EMIT_ON_NORMAL=false

# 市场 ATR 周期
# 实盘建议值：14（默认值，标准 ATR 周期）
MARKET_ATR_PERIOD=14

# 新闻窗口（UTC 时间，格式：HH:MM-HH:MM）
# 例如：08:00-10:00 表示 UTC 8点到10点
# 实盘建议值：空（默认值，不设置新闻窗口）
NEWS_WINDOW_UTC=

# =============================================================================
# 数据质量检查配置
# =============================================================================

# 是否启用数据质量检查
# 实盘建议值：true（默认值，确保数据质量）
DATA_QUALITY_ENABLED=true

# 数据延迟告警阈值（毫秒）
# 超过此时间的数据将被标记为延迟
# 实盘建议值：180000（3分钟，默认值）
DATA_QUALITY_LAG_MS=180000

# 价格跳变告警阈值（百分比）
# 超过此百分比的价格变化将被标记为异常
# 实盘建议值：0.08（8%，默认值）
DATA_QUALITY_PRICE_JUMP_PCT=0.08

# 成交量异常倍数
# 超过平均成交量此倍数的将被标记为异常
# 实盘建议值：10.0（默认值）
DATA_QUALITY_VOLUME_SPIKE_MULTIPLE=10.0

# 成交量统计窗口（K 线数量）
# 实盘建议值：30（默认值）
DATA_QUALITY_VOLUME_WINDOW=30

# =============================================================================
# 账户熔断配置（重要：风险控制）
# =============================================================================

# 是否启用账户级熔断
# 实盘建议值：true（强烈建议启用，保护账户）
ACCOUNT_KILL_SWITCH_ENABLED=false

# 每日亏损限制（百分比）
# 超过此百分比将触发熔断
# 实盘建议值：0.03（3%，保守）到 0.05（5%，适中）
# 警告：不要设置过大，避免单日亏损过多
DAILY_LOSS_LIMIT_PCT=0.03

# 账户熔断强制开启（用于紧急停止）
# 实盘建议值：false（默认值，仅在紧急情况下设置为 true）
ACCOUNT_KILL_SWITCH_FORCE_ON=false

# 熔断标志名称（Redis 键名）
# 实盘建议值：KILL_SWITCH（默认值）
KILL_SWITCH_FLAG_NAME=KILL_SWITCH

# 熔断窗口（毫秒）
# 实盘建议值：300000（5分钟，默认值）
KILL_SWITCH_WINDOW_MS=300000

# =============================================================================
# 风险监控配置
# =============================================================================

# 是否启用风险熔断
# 实盘建议值：true（强烈建议启用）
RISK_CIRCUIT_ENABLED=false

# 每日回撤软阈值（百分比）
# 超过此阈值将触发软熔断（停止新开仓）
# 实盘建议值：0.02（2%，保守）
DAILY_DRAWDOWN_SOFT_PCT=0.02

# 每日回撤硬阈值（百分比）
# 超过此阈值将触发硬熔断（强制平仓）
# 实盘建议值：0.04（4%，保守）
DAILY_DRAWDOWN_HARD_PCT=0.04

# 风险监控间隔（秒）
# 实盘建议值：10.0（默认值，平衡实时性和系统负载）
RISK_MONITOR_INTERVAL_SEC=10.0

# =============================================================================
# 执行服务配置
# =============================================================================

# 执行模式
# 可选值：LIVE（实盘）/ PAPER（模拟盘）/ BACKTEST（回测）
# 实盘建议值：LIVE（实盘交易）
# 警告：确保在实盘前充分测试 PAPER 模式
EXECUTION_MODE=LIVE

# 入场订单类型
# 可选值：Market（市价单）/ Limit（限价单）
# 实盘建议值：Market（默认值，快速成交）
# 注意：Limit 模式支持超时重试，但可能错过快速行情
EXECUTION_ENTRY_ORDER_TYPE=Market

# 入场订单超时时间（毫秒）
# 实盘建议值：15000（15秒，默认值）
EXECUTION_ENTRY_TIMEOUT_MS=15000

# 入场订单最大重试次数
# 实盘建议值：2（默认值）
EXECUTION_ENTRY_MAX_RETRIES=2

# 入场订单重试价格调整（基点，1基点=0.01%）
# 每次重试时价格调整幅度
# 实盘建议值：5（默认值，5基点）
EXECUTION_ENTRY_REPRICE_BPS=5

# 入场订单失败后是否回退到市价单
# 实盘建议值：true（默认值，确保成交）
EXECUTION_ENTRY_FALLBACK_MARKET=true

# 入场订单部分成交超时时间（毫秒）
# 实盘建议值：20000（20秒，默认值）
EXECUTION_ENTRY_PARTIAL_FILL_TIMEOUT_MS=20000

# 订单轮询间隔（秒）
# 实盘建议值：1.0（默认值）
ORDER_POLL_INTERVAL_SEC=1.0

# 订单轮询超时时间（秒）
# 实盘建议值：20.0（默认值）
ORDER_POLL_TIMEOUT_SEC=20.0

# 订单超时告警窗口（毫秒）
# 用于去重订单超时告警
# 实盘建议值：60000（1分钟，默认值）
ORDER_TIMEOUT_ALERT_WINDOW_MS=60000

# =============================================================================
# 持仓管理配置
# =============================================================================

# 是否启用冷却期
# 实盘建议值：true（默认值，避免频繁交易）
COOLDOWN_ENABLED=true

# 1小时时间框架冷却期（K 线数量）
# 实盘建议值：2（默认值）
COOLDOWN_BARS_1H=2

# 4小时时间框架冷却期（K 线数量）
# 实盘建议值：1（默认值）
COOLDOWN_BARS_4H=1

# 1天时间框架冷却期（K 线数量）
# 实盘建议值：1（默认值）
COOLDOWN_BARS_1D=1

# 同币种同向持仓互斥升级动作
# 可选值：BLOCK（拒绝）/ CLOSE_LOWER_AND_OPEN（先平低优先级再开仓）
# 实盘建议值：CLOSE_LOWER_AND_OPEN（默认值，允许升级）
POSITION_MUTEX_UPGRADE_ACTION=CLOSE_LOWER_AND_OPEN

# =============================================================================
# 跟随止损（Runner）配置
# =============================================================================

# 跟随止损模式
# 可选值：ATR（ATR 跟随）/ PIVOT（枢轴点跟随）
# 实盘建议值：ATR（默认值）
RUNNER_TRAIL_MODE=ATR

# Runner ATR 周期
# 实盘建议值：14（默认值，标准 ATR 周期）
RUNNER_ATR_PERIOD=14

# Runner ATR 倍数
# 实盘建议值：3.0（默认值）
RUNNER_ATR_MULT=3.0

# 是否启用 Runner 实时更新
# 实盘建议值：true（默认值，提高止损精度）
RUNNER_LIVE_UPDATE_ENABLED=true

# Runner 实时更新最小间隔（毫秒）
# 实盘建议值：3000（3秒，默认值）
RUNNER_LIVE_UPDATE_MIN_INTERVAL_MS=3000

# =============================================================================
# 执行增强功能配置
# =============================================================================

# 是否启用次要规则
# 实盘建议值：true（默认值）
SECONDARY_RULE_ENABLED=true

# 是否启用订单重试
# 实盘建议值：true（默认值）
ENABLE_ORDER_RETRY=true

# 是否启用信号评分（仅用于复盘，不改变交易行为）
# 实盘建议值：false（默认值）
ENABLE_SIGNAL_SCORING=false

# 是否启用市场状态标记
# 实盘建议值：false（默认值）
ENABLE_MARKET_STATE=false

# =============================================================================
# 一致性检查配置
# =============================================================================

# 是否启用 WS/DB 一致性漂移检测
# 实盘建议值：true（默认值，确保数据一致性）
CONSISTENCY_DRIFT_ENABLED=true

# 一致性漂移阈值（百分比）
# 实盘建议值：0.10（10%，默认值）
CONSISTENCY_DRIFT_THRESHOLD_PCT=0.10

# 一致性漂移窗口（毫秒）
# 实盘建议值：300000（5分钟，默认值）
CONSISTENCY_DRIFT_WINDOW_MS=300000

# 是否启用钱包 WS+REST 对比
# 实盘建议值：true（默认值，确保钱包数据准确性）
WALLET_COMPARE_ENABLED=true

# 钱包 WS 数据最大年龄（毫秒）
# 超过此时间的 WS 数据将被视为过期
# 实盘建议值：90000（90秒，默认值）
WALLET_WS_MAX_AGE_MS=90000

# 钱包漂移阈值（百分比）
# 实盘建议值：0.02（2%，默认值）
WALLET_DRIFT_THRESHOLD_PCT=0.02

# 钱包漂移窗口（毫秒）
# 实盘建议值：300000（5分钟，默认值）
WALLET_DRIFT_WINDOW_MS=300000

# 对账：未完成订单轮询间隔（秒）
# 实盘建议值：5.0（默认值，当启用私有 WS 时可适当增大）
RECONCILE_OPEN_ORDERS_POLL_INTERVAL_SEC=5.0

# =============================================================================
# 通知服务配置
# =============================================================================

# Telegram Bot Token（可选，用于 Telegram 通知）
# 实盘建议值：如果使用 Telegram 通知，从 @BotFather 获取
TELEGRAM_BOT_TOKEN=

# Telegram Chat ID（可选，用于 Telegram 通知）
# 实盘建议值：如果使用 Telegram 通知，从 @userinfobot 获取
TELEGRAM_CHAT_ID=

# 通知最大重试次数
# 实盘建议值：5（默认值）
NOTIFIER_MAX_ATTEMPTS=5

# 通知重试循环间隔（秒）
# 实盘建议值：5.0（默认值）
NOTIFIER_RETRY_LOOP_INTERVAL_SEC=5.0

# =============================================================================
# 监控和告警配置
# =============================================================================

# 是否启用流延迟告警
# 实盘建议值：true（默认值，及时发现处理延迟）
ALERT_STREAM_LAG_ENABLED=true

# 交易计划延迟告警阈值（毫秒）
# 实盘建议值：60000（1分钟，默认值）
ALERT_TRADE_PLAN_LAG_MS=60000

# K 线收盘延迟告警阈值（毫秒）
# 实盘建议值：120000（2分钟，默认值）
ALERT_BAR_CLOSE_LAG_MS=120000

# 账户快照间隔（秒）
# 实盘建议值：30.0（默认值）
ACCOUNT_SNAPSHOT_INTERVAL_SEC=30.0

# =============================================================================
# 管理配置
# =============================================================================

# 管理员令牌（用于 API 管理接口）
# 实盘建议值：设置强密码，不要使用默认值
# 安全提示：不要将管理员令牌提交到代码仓库
ADMIN_TOKEN=

# =============================================================================
# 回测和模拟盘配置
# =============================================================================

# 回测/模拟盘初始资金（USDT）
# 用于 PAPER 和 BACKTEST 模式的仓位计算
# 实盘建议值：10000（默认值，根据测试需求调整）
BACKTEST_EQUITY=10000

# 模拟盘初始资金（USDT）
# 如果设置了 BACKTEST_EQUITY，则优先使用 BACKTEST_EQUITY
# 实盘建议值：10000（默认值）
PAPER_EQUITY=10000

# =============================================================================
# 初始化控制配置
# =============================================================================

# 是否跳过数据库迁移（用于调试）
# 实盘建议值：0（默认值，确保数据库结构正确）
# 设置为 1 可跳过迁移（不推荐）
SKIP_DB_MIGRATIONS=0

# 是否跳过 Redis Streams 初始化（用于调试）
# 实盘建议值：0（默认值，确保 Streams 正确初始化）
# 设置为 1 可跳过初始化（不推荐）
SKIP_REDIS_STREAMS_INIT=0

# =============================================================================
# 配置说明
# =============================================================================
# 1. 实盘交易前必须配置：
#    - BYBIT_API_KEY 和 BYBIT_API_SECRET（实盘交易）
#    - DATABASE_URL 和 REDIS_URL（数据库和消息队列）
#    - EXECUTION_MODE=LIVE（实盘模式）
#    - ENV=prod（生产环境）
#
# 2. 风险控制参数建议：
#    - RISK_PCT: 0.005（0.5%）到 0.01（1%）
#    - MAX_OPEN_POSITIONS: 3（根据资金量调整）
#    - DAILY_LOSS_LIMIT_PCT: 0.03（3%）
#    - ACCOUNT_KILL_SWITCH_ENABLED: true（强烈建议）
#    - RISK_CIRCUIT_ENABLED: true（强烈建议）
#
# 3. 性能优化建议：
#    - BYBIT_PRIVATE_WS_ENABLED: true（减少 REST API 压力）
#    - BYBIT_PRIVATE_ACTIVE_SYMBOLS_ONLY: true（减少查询压力）
#    - 合理设置限流参数（避免触发 Bybit 限流）
#
# 4. 监控建议：
#    - ALERT_STREAM_LAG_ENABLED: true
#    - DATA_QUALITY_ENABLED: true
#    - WALLET_COMPARE_ENABLED: true
#
# 5. 安全建议：
#    - 使用独立的数据库和 Redis 实例
#    - API Key 只授予必要的权限（不要授予提现权限）
#    - 定期更换 API Key 和 Secret
#    - 不要将敏感信息提交到代码仓库
# =============================================================================
